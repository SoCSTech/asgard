apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "asgard.fullname" . }}-mosquitto-config
  labels:
    {{- include "asgard.labels" . | nindent 4 }}
data:
  mosquitto.conf: |-
{{ .Values.mosquittoConfig | indent 4 }}
